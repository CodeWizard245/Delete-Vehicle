Install:
 - Create a new folder in your resources folder named `Dv`
 - Extract the archive's contents to your Dv` folder.
 - ensure `Dv`resource in your ``server.cfg``.

Command default:
 - dv(delete vehicle)
